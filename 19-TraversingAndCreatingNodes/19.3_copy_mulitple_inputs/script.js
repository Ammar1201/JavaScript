
const inputs = document.querySelectorAll('.input');
// console.log(inputs);

// const copiedData = ClipboardEvent.clipboardData();

// const copiedData = new DataTransfer(ClipboardEvent.clipboardData);
// console.log(copiedData);

const verficationCode = document.querySelectorAll('label')[1];
// console.log(label);
verficationCode.addEventListener('copy', (event) => {
  
});

verficationCode.addEventListener('paste', (event) => {

});